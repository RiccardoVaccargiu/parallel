import { makeStyles } from "@material-ui/core/styles";

export const homepageStyle = makeStyles((theme) => ({

    mainBox: {
        minHeight: "100vh",
        display: "flex",
        flexDirection: "row",
    }
}))